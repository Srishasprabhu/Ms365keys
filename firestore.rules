rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Since authentication was removed, these rules allow public read/write access.
    // This is insecure and only recommended for prototyping.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
